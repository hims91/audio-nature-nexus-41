
// This file is kept for backward compatibility but now uses the unified types
export type { ExternalLink, PortfolioItem } from "@/types/portfolio";

// Re-export the portfolio items array for any components that still need it
export const portfolioItems = [];
